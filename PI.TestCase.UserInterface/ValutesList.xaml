<Page
    x:Class="PI.TestCase.UserInterface.ValutesList"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PI.TestCase.UserInterface"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" Loaded="Page_Loaded">

    <Grid Width="300">
        <ListView IsItemClickEnabled="True" ItemsSource="{x:Bind vm.Data}" ItemClick="ListView_ItemClick">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="local:Valute">
                    <StackPanel Orientation="Horizontal"  >
                        <TextBlock Text="{x:Bind Name}" HorizontalAlignment="Left" Width="240" TextWrapping="Wrap"/>
                        <TextBlock Text="{x:Bind CharCode}" HorizontalAlignment= "Right" Width="35"/>
                    </StackPanel> 
                </DataTemplate>
            </ListView.ItemTemplate> 
        </ListView>
    </Grid>
</Page>
